# Embedded local mode (mvn spring-boot:run): keep outbox off until DDL is applied manually.
account:
  award-credit-outbox:
    enabled: false

job:
  shared-task-dispatch:
    credit-award-disabled: false
