# Embedded local mode (mvn spring-boot:run) uses the local account adapters selected by
# the local/dev Spring Profile. The credit award Outbox is always enabled; apply its DDL
# before starting message-job.
