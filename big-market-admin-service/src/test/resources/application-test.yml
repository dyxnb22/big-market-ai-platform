spring:
  main:
    lazy-initialization: false

logging:
  file:
    path: target/test-logs

nacos:
  config:
    sync:
      enabled: false

default-credential-guard:
  enabled: false

redis:
  sdk:
    config:
      host: 127.0.0.1
      port: 16379
