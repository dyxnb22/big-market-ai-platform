---
description: Historical note — Java sources removed
alwaysApply: false
---

# Java conventions (retired)

Java/Maven modules were deleted from this repository. See `rust-refactor/JAVA-DELETION-LEDGER.md`.

Do not add new `.java` files or a root `pom.xml` unless the user explicitly requests restoring a Java track.
