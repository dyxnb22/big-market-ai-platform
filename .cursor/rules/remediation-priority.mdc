---
description: Remediation priority — Rust-first after Java removal
alwaysApply: true
---

# Remediation priority

Java Spring sources were **removed**. Prefer Rust fixes in `big-market-rs/`.

1. Money-path / idempotency / outbox correctness (`docs/data-and-outbox.md`, skill `money-path-change`).
2. Boot / worker / gateway regressions (`bm-app`, `bm-worker`, `bm-gateway`).
3. MySQL backend (`BM_BACKEND=mysql`) and smoke scripts.
4. Docs sync when readiness claims change (`docs-sync` skill).

Historical BM-001… plan in `docs/audit-remediation-plan.md` is **clue only** (pre-deletion Java audit). Do not restore Java modules to “fix” BM items unless the user explicitly requests a Java track.
