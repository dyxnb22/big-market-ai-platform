<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.dyx.market</groupId>
        <artifactId>big-market</artifactId>
        <version>1.1</version>
    </parent>

    <artifactId>big-market-activity-service</artifactId>

    <!--
        Phase 5-F dark-launch scaffold.

        This module is the future home of the activity bounded context. In this
        batch it is a minimal Spring Boot launcher with a constrained scan scope.
        No Dubbo provider, no HTTP controller, no MQ listener, no XXL-Job handler,
        and no mapper XML are added here. big-market-trigger is intentionally
        excluded from dependencies and from scanBasePackages.

        Draw execution (RaffleApplicationService) remains in big-market-market-service
        until Phase 5-G saga/outbox design is approved and Phase 7 table ownership
        is resolved.
    -->

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.dubbo</groupId>
            <artifactId>dubbo</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.dubbo</groupId>
            <artifactId>dubbo-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.nacos</groupId>
            <artifactId>nacos-client</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Project modules (big-market-trigger intentionally excluded) -->
        <dependency>
            <groupId>com.dyx.market</groupId>
            <artifactId>big-market-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.dyx.market</groupId>
            <artifactId>big-market-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>com.dyx.market</groupId>
            <artifactId>big-market-infrastructure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.dyx.market</groupId>
            <artifactId>big-market-types</artifactId>
        </dependency>
        <dependency>
            <groupId>com.dyx.market</groupId>
            <artifactId>big-market-starter-db-router</artifactId>
        </dependency>
    </dependencies>

    <build>
        <finalName>big-market-activity-service</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>**/**</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>2.6.0</version>
                <configuration>
                    <mainClass>com.dyx.market.activity.ActivityServiceApplication</mainClass>
                    <layout>JAR</layout>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
